apiVersion: v1
kind: Service
metadata:
  name: processing-service
  namespace: aerostream
spec:
  selector:
    app: aerostream-processing
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP